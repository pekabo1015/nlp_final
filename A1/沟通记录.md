# 沟通记录

## 项目目标
使用 Python 开发一个 Web 应用，实现中文分词与词性标注的结合。

## 沟通记录

### 2026-03-11
- 用户提出初始需求：先建立一个专门记录沟通过程的 `md` 文档。
- 用户提出初始需求：新建一个空白的 `.py` 文件，用于后续代码编写。
- 当前已创建：`沟通记录.md`、`main.py`。
- 用户补充 Web 应用整体方案：页面需包含中文长文本输入框，以及三个并排展示区，分别用于文本规范化、中文分词与词频可视化、词性标注着色展示。
- 当前已完成：在 `main.py` 中实现单文件 Web 应用原型。

### 2026-03-12
- 用户确认已在本地跑通 `main.py`（依赖：flask、jieba、opencc-python-reimplemented，已提供 `requirements.txt`）。
- 用户提出后续优化流程：① 将沟通记录续写在 `沟通记录.md`；② 在 `main.py` 基础上按用户修改要求改动，将修改后的代码写入 `main_updated.md`。
- 待用户提供对 Web 应用的具体修改要求后，按上述流程执行。
- 用户提出优化需求：① 在原有分词外再设计 3–5 种不同分词算法，进行效果对比（可借助其他库），要求对易产生歧义的句子在分词结果上存在差异；② 对不同算法的分词效果做统计分析，提供统计指标并以可视化方式呈现。修改后的代码写入 `main_updated.md`。
- 已完成：在 `main_updated.md` 中实现多算法分词对比与统计可视化。包含 5 种分词（jieba 精确/全模式/搜索引擎、pkuseg、SnowNLP）、统计表（词数、平均词长、唯一词数）、词数柱状图与算法间 Jaccard 相似度热力图。可选依赖：`pkuseg`、`snownlp`。
- 用户反馈本地无法安装 `pkuseg`，已自行修改 `requirements.txt`。仅在 `main_updated.md` 中移除 pkuseg 相关逻辑，保留四种分词算法：jieba 精确模式、jieba 全模式、jieba 搜索引擎模式、SnowNLP；未改动任何 `.py` 文件。
- 用户提出 UI 优化：仅在 `main_updated.md` 中调整。① 输入框位置保持页面上方不变；② 五区块改为两栏布局：左侧从上到下为区块1、2、3（标准 jieba 结果），右侧为区块4、5（多算法对比与统计可视化），两栏底部对齐；③ 界面采用圆角卡片、无边框柔和边界、卡片内轻量进度条，整体柔和明亮风格。
- 用户要求还原上下分栏布局：上一栏为一个大卡片，从左到右为区块1、2、3，卡片左上角小标题「标准 jieba 分词结果」；下一栏为一个大卡片，上下排列区块4、5，卡片左上角小标题「多算法对比与统计分析」。均在 `main_updated.md` 中修改。
- 用户要求继续优化 UI（仅改 `main_updated.md`）：① 两个大卡片圆角更明显、加轻微阴影区分层级；② 小标题前加竖线/色块，区块标题（区块1～5）形成大中小三级标题层级；③ 每个区块外加小卡片，风格与大卡片一致。
- 用户要求：区块小卡片内部左右边距适当加宽；条形图柱体适当改窄。已在 `main_updated.md` 中完成。
- 用户要求：区块5 内「各算法分词数量对比」由垂直条形图改为横向条形图。已在 `main_updated.md` 中通过 Chart.js `indexAxis: "y"` 实现。
- 用户要求：区块5 内「各算法分词数量对比」与「算法间 Jaccard 相似度」两表高度对齐、尺寸长宽固定一致。已在 `main_updated.md` 中通过固定 `.chart-block` 高度、左侧图表加 `.chart-body` 容器与 `maintainAspectRatio: false` 实现。
